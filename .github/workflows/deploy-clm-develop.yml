# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Test Environment

on:
  pull_request_target:
  push:
    branches:
      - develop

jobs:
  call-workflow-deploy-clm:
    uses: ./.github/workflows/reusable-deploy-project-workflow.yml
    with:
      environment: elewa-dev
      project_id: elewa-clm-test
      channelId: live
    secrets:
     FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_ELEWA_DEV}}
     ENVIRONMENT_FILE: ${{ secrets.ENVIRONMENT_FILE_ELEWA_DEV }}
