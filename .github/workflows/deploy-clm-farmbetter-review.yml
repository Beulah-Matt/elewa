# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Farmbetter Review
'on':
  push:
    branches:
      - main
jobs:
  call-workflow-deploy-clm:
    uses: ./.github/workflows/reusable-deploy-project-workflow.yml
    with:
      environment: farmbetter-review
      project_id: farmbetter-review
      channelId: live
      target: convl-manager
    secrets:
      FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_FARMBETTER_REVIEW }}
      ENVIRONMENT_FILE: ${{ secrets.ENVIRONMENT_FILE_FARMBETTER_REVIEW }}
