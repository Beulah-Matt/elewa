# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Elewa Production
'on':
  push:
    branches:
      - main
jobs:
  call-workflow-deploy-clm:
    uses: ./.github/workflows/reusable-deploy-project-workflow.yml
    with:
      environment: elewa-prod
      project_id: elewa-conv-learning-prod
      channelId: live
    secrets:
      FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_ELEWA_CONV_LEARNING_PROD}}
      ENVIRONMENT_FILE: ${{ secrets.ENVIRONMENT_FILE_ELEWA_CONV_LEARNING_PROD }}
